import 'package:flutter/material.dart';

class GenerateModel extends StatelessWidget {
  const GenerateModel({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Generate model page'),
    );
  }
}
