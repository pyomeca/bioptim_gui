import 'package:flutter/material.dart';

class LoadExisting extends StatelessWidget {
  const LoadExisting({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Load existing solution page'),
    );
  }
}
